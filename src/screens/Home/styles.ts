import {Appbar} from 'react-native-paper';
import styled from 'styled-components/native';

export const HomeContainer = styled.View`
  flex: 1;
`;

export const HeaderBar = styled(Appbar.Header)`
  elevation: 0;
`;
